# Examples

- `83300100-DailyInstDirectNew.md`
  - Recommended primary example for this skill.
  - Shows the expected style for:
    - explicit scope statement in `Summary` and matching skipped branches in `TODO`
    - detailed `Involved tables` with read/write evidence
    - grouped table listing (`读取表` / `写入表`) and conditional markers with trigger reasons
    - `Procedure`-first structure with leaf-method detail merged in place
    - leaf detail that includes short-circuit/no-op behavior, key-miss handling, and concrete check formulas
    - dynamic-dispatch resolution and active-branch-only conditional compilation notes
